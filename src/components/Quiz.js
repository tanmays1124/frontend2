import react from 'react';
import { useParams } from 'react-router-dom';


const Quiz = ({question,setQuestion}) => {
    console.log(title)
  
    return (
      <>
        <h1>Quiz {question}</h1>
      </>
    );
  };
  
export default Quiz;